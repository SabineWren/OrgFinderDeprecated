
DB Population:
	* delete orgs that no longer exist
	***** fix weekly growth calculation

Misc:
	the SC-API does not seem to scrape exclusive status (need workaround)
	
Front End:
	build a webgl version of the cube
	add banner/footer
	*** improve fonts

